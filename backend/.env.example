# Copy to .env and adjust as needed.

# SQLAlchemy async URL. Default is a local SQLite file next to this project.
DATABASE_URL=sqlite+aiosqlite:///./app.db

# Log SQL statements (verbose; useful for debugging).
DB_ECHO=false

# JSON array of allowed CORS origins.
CORS_ORIGINS=["*"]

# PatientReplyGenerator backing POST /sessions/{id}/messages is the external RAG
# patient-chat API (app/infrastructure/rag_patient_generator.py) -- not configurable.
# A local Qwen2.5-0.5B-Instruct model used to be a selectable alternative here; removed
# after live reproduction confirmed it hallucinates/breaks character systematically.
# See RAG_API_BASE_URL/RAG_API_KEY below (same service as the retrieval-only endpoint)
# and the RAG_PATIENT_* settings further down for how that one backend is tuned.

# Which EvaluationGenerator backs POST /sessions/{id}/evaluate:
#   rag_llm -- the default. LLM-judge backed by the external RAG API's clinician-chat route
#              (POST /v1/rag/chat, app/infrastructure/rag_llm_evaluator.py) -- same service/
#              base URL/key as RAG_API_BASE_URL/RAG_API_KEY below. Runs asynchronously; see
#              app/application/use_cases/evaluate_session_async.py.
#   stub    -- DEPRECATED escape hatch, kept only until rag_llm is confirmed working in
#              production (app/infrastructure/stub_evaluator.py); deterministic, rule-based
#              placeholder grading, no ML/network dependency.
EVALUATION_BACKEND=rag_llm

# Read/hard timeouts (seconds) for one evaluation call to /v1/rag/chat -- see
# Settings.rag_evaluation_read_timeout_seconds / rag_evaluation_hard_timeout_seconds in
# app/config.py for how these were picked.
RAG_EVALUATION_READ_TIMEOUT_SECONDS=90
RAG_EVALUATION_HARD_TIMEOUT_SECONDS=100

# Which SpeechToTextPort backs POST /transcribe, POST /sessions/{id}/chat-voice, and WS /ws/voice:
#   colab     -- HTTP call to the self-hosted Whisper model exposed by the Colab notebook at
#                colab/leva_stt_tts_notebook.py (app/infrastructure/colab_stt_adapter.py). Needs
#                COLAB_API_BASE_URL set below.
#   whisper   -- DEPRECATED, kept only for rollback. Local faster-whisper "tiny" model, forced
#                Arabic, CPU-only (app/infrastructure/legacy/local_whisper_stt_adapter.py).
#                Downloads ~75MB of weights on first use; falls back to the stub adapter
#                automatically if unavailable. NOT the same as local_gpu below.
#   local_gpu -- The exact same openai-whisper "small" model the Colab notebook runs, loaded
#                in-process on CUDA at startup instead of called over HTTP
#                (app/infrastructure/local_gpu_stt_adapter.py + voice_models.py). Forces an
#                attempt regardless of LOCAL_GPU_MIN_FREE_VRAM_GB below -- falls back to the
#                same behavior as "colab" automatically (never crashes) if no GPU is present or
#                the load fails for any reason. Needs torch (CUDA build) + openai-whisper
#                installed -- see requirements.txt.
#   auto      -- Same local model as local_gpu, but only attempted if the startup VRAM check
#                clears LOCAL_GPU_MIN_FREE_VRAM_GB first; falls straight to "colab" behavior
#                otherwise. Recommended default on a machine with a capable GPU.
#   stub      -- fixed placeholder transcript, no ML dependency (what the test suite uses by
#                default -- see tests/conftest.py).
STT_BACKEND=auto

# Which TextToSpeechPort backs the same endpoints:
#   colab     -- HTTP call to the self-hosted leva-tts model exposed by the Colab notebook at
#                colab/leva_stt_tts_notebook.py (app/infrastructure/colab_tts_adapter.py). Needs
#                COLAB_API_BASE_URL set below.
#   gtts      -- DEPRECATED, kept only for rollback. gTTS (app/infrastructure/legacy/local_tts_adapter.py).
#                Note this makes a network call to Google Translate's TTS endpoint per request --
#                not offline. Falls back to the stub adapter's silent placeholder WAV automatically
#                if unavailable.
#   local_gpu -- The exact same leva-tts model/default speaker ("Amina") the Colab notebook runs,
#                loaded in-process on CUDA at startup instead of called over HTTP
#                (app/infrastructure/local_gpu_tts_adapter.py + voice_models.py). Decided
#                independently of STT_BACKEND's local_gpu/auto (one can end up local while the
#                other falls back to Colab). Needs torch (CUDA build) + leva-tts installed, AND
#                the espeak-ng/ffmpeg/libsndfile1 SYSTEM packages leva-tts depends on (see
#                colab/leva_stt_tts_notebook.py CELL 1) -- missing system deps are detected at
#                startup and treated as a load failure, falling back to "colab" automatically.
#   auto      -- Same local model as local_gpu, gated by the same startup VRAM check as STT_BACKEND's
#                auto above. Recommended default on a machine with a capable GPU.
#   stub      -- silent placeholder WAV, no network/ML dependency (what the test suite uses by
#                default).
TTS_BACKEND=auto

# Minimum free VRAM (GB), checked once at startup via torch.cuda.mem_get_info(), for
# STT_BACKEND=auto / TTS_BACKEND=auto to choose local GPU loading over the Colab fallback. Whisper
# "small" + leva-tts together measured ~3.5GB combined on Colab's T4 -- this sits above that
# figure with headroom for CUDA context overhead/fragmentation/other GPU users, not right at it.
# Not consulted at all when STT_BACKEND/TTS_BACKEND is "local_gpu" (forced) rather than "auto".
LOCAL_GPU_MIN_FREE_VRAM_GB=4.5

# Base URL of the Colab-hosted STT/TTS API (see colab/leva_stt_tts_notebook.py), used when
# STT_BACKEND and/or TTS_BACKEND above is "colab". This is the ngrok URL printed at the bottom
# of the Colab notebook's last cell -- it changes every time the notebook is restarted (a free
# ngrok tunnel gets a new random URL each run), so just update this line, no code changes needed.
# No trailing slash. Example: COLAB_API_BASE_URL=https://xxxx-xx-xx-xxx-xx.ngrok-free.app
COLAB_API_BASE_URL=https://synopsis-affidavit-debate.ngrok-free.dev

# Per-request timeout (seconds) for calls to the Colab API above. Generous by default since this
# is a real network hop through ngrok to a Colab GPU runtime, which can be slow or drop entirely.
COLAB_API_TIMEOUT_SECONDS=30

# Base URL of the external RAG (retrieval-only) API (app/infrastructure/rag_client_adapter.py).
# No trailing slash. Example: RAG_API_BASE_URL=https://vr-rag-api.nport.link
RAG_API_BASE_URL=

# API key for the RAG API, sent as the X-API-Key header. Fill this in yourself -- never commit a
# real value here or anywhere else in this repo.
RAG_API_KEY=

# Connect/read timeouts (seconds) for calls to the RAG API above.
RAG_API_TIMEOUT_CONNECT_SECONDS=5
RAG_API_TIMEOUT_READ_SECONDS=60

# Retry policy for the RAG API: retry transient 503s with backoff, never retry
# 401/422 (see app/infrastructure/rag_client_adapter.py). Total attempts includes
# the first try, e.g. 3 = 1 initial attempt + up to 2 retries.
RAG_API_MAX_ATTEMPTS=3
RAG_API_RETRY_BACKOFF_SECONDS=1

# Patient-chat (POST /v1/rag/patient/chat) settings -- app/infrastructure/rag_patient_generator.py.
# Same RAG_API_BASE_URL/RAG_API_KEY above; this is a different route on the same service.
RAG_PATIENT_PERSONA=middle_man
RAG_PATIENT_READ_TIMEOUT_SECONDS=60
# PostMessageAsyncUseCase's hard deadline for one generate_reply() call (adapter-internal
# retries included) -- slightly above RAG_PATIENT_READ_TIMEOUT_SECONDS on purpose.
RAG_PATIENT_HARD_TIMEOUT_SECONDS=65
# How long to wait before pushing an interim "still thinking" WS event if the real reply
# isn't ready yet -- see app/application/use_cases/post_message_async.py.
RAG_PATIENT_FILLER_AFTER_SECONDS=15
# Fixed, in-character fallback line -- pushed as the final reply if generation times out
# or fails. Deliberately generic; never claims specific symptoms or breaks character.
RAG_PATIENT_FALLBACK_REPLY=آسف، هل يمكنك إعادة السؤال؟ شردت شوي.
