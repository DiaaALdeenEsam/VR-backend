fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sqlmodel==0.0.22
sqlalchemy==2.0.35
aiosqlite==0.20.0
alembic==1.13.3
python-dotenv==1.0.1

# CPU-only wheels for the local Qwen patient-reply generator (app/infrastructure/qwen_patient_generator.py).
# A 0.5B model doesn't need a GPU, and CPU wheels avoid CUDA/driver version matching -- more
# portable for a dev/test-only component. Drop this line (and torch's `+cpu` pin below) if you'd
# rather use a CUDA build on a machine where that's already set up.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1
transformers==4.46.3
accelerate==1.0.1

python-multipart==0.0.12  # required by FastAPI for UploadFile / multipart form parsing (voice endpoints)

# Local voice pipeline (app/infrastructure/local_whisper_stt_adapter.py, local_tts_adapter.py).
faster-whisper==1.0.3      # STT: CTranslate2-based, no system ffmpeg needed (bundled via the `av` wheel)
gtts==2.5.4                # TTS: tiny pure-Python client for Google Translate's TTS endpoint (network call
                            # per request, not offline -- see local_tts_adapter.py's docstring)
miniaudio==1.71            # TTS: decodes gTTS's MP3 output to raw PCM so local_tts_adapter.py can re-encode
                            # it as WAV -- ships prebuilt wheels, no system ffmpeg needed

httpx==0.27.2
pytest==8.3.3
pytest-asyncio==0.24.0
